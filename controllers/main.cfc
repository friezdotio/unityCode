component accessors="true" extends="base"{

    public function init(fw) {
        variables.fw = fw;
    }

    /*
    	Display Pages
    */

    function default(rc){

	   /* if(structKeyExists(rc, "populate") AND rc.populate EQ "walkon"){
	   		variables.baseServices.populateInital();
	    }*/

    }

    /*
    	Action Pages
    */

}
